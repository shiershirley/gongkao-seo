# 2026-04-28 工作记录

## SEO优化代码升级（commit: 04d3c86）

基于谷歌SEO避坑指南文章，对照网站现状执行了以下优化：

### 已完成修改（4个文件）
1. **`src/app/[category]/[slug]/page.tsx`**（文章详情页）
   - 添加 `generateStaticParams()`：127篇文章全部 SSG 预渲染（原为 SSR）
   - 添加 Article JSON-LD 结构化数据（@type: Article，含 headline/author/publisher/datePublished/keywords）
   - 显式声明 `alternates.canonical`
   - 添加 Twitter Card（summary_large_image）
   - 升级 Markdown 渲染器：支持有序列表（`1.`）和表格（`|`语法）

2. **`src/app/[category]/page.tsx`**（分类列表页）
   - 显式声明 `alternates.canonical`
   - 添加 Twitter Card

3. **`src/app/page.tsx`**（首页）
   - 添加首页 metadata（canonical + Twitter Card）
   - 修复热搜关键词区：原来指向 `?q=xxx` query string（爬虫无法收录），改为指向真实分类页

### 未做的优化（留待后续）
- WebSite / Organization JSON-LD（品牌信号，低优先级）
- OG 默认封面图（需设计图片资源）
- 时效性文章定期更新（内容层面，非代码层面）

## 谷歌SEO避坑指南核心要点（参考原文）
- AI内容占比安全线：工具类≤38%，创意类≤15%
- 文章页安全字数：≥500词（Top10均值1447词）
- 时效性：frontmatter用 datePublished + dateModified
- 结构化数据是从关键词匹配转向语义理解的关键
- 热搜关键词区不能用 query string，必须指向可收录的真实页面
